### About
A simple re-implementation of SecureChat app.
Only support for one-to-one connection between servers and clients.

### Installation
`pip install requirements.txt`

### Running
`python smc.py`